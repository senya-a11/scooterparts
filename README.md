# ⚡ IMPORT v8.3

Интернет-магазин премиум запчастей для электросамокатов. FastAPI + PostgreSQL + Jinja2.

**🎉 Новое в v8.3:**
- ✅ **Исправлена загрузка изображений** — PNG/JPG теперь корректно загружаются и отображаются
- 🖼️ **Автоматическая оптимизация** — все изображения сжимаются и ресайзятся
- ⚡ **Lazy loading** — быстрая загрузка страниц
- 📸 **Превью в админке** — предпросмотр перед загрузкой
- ✅ **Валидация файлов** — проверка формата и размера (макс 10MB)
- 📱 **Bottom Sheet меню** — мобильное меню админки выезжает снизу
- 🔍 **SEO-оптимизация** — мета-теги, Open Graph, alt-тексты
- 🎨 **Унифицированные шрифты** — правильные шрифты на всех карточках
- 📦 **Поддержка всех форматов** — PNG, JPG, JPEG, WEBP, GIF

**Из предыдущих версий:**
- ⚡ Ребрендинг: ScooterParts → IMPORT (новый логотип)
- 📦 Поддержка предзаказа (товар может быть И в наличии, И по предзаказу)
- 🛒 Изменение количества товаров в корзине (кнопки +/- и поле ввода)
- 🔐 Редирект на регистрацию при добавлении в корзину без авторизации
- 🎨 Favicon (буква "I" из логотипа IMPORT)

📄 **Подробный список изменений:** см. `CHANGELOG_v8.3.md`

---

## 🚀 Быстрый старт (локально)

```bash
# 1. Клонируйте или распакуйте архив
# 2. Скопируйте .env.example → .env и заполните
cp .env.example .env

# Через Docker Compose (рекомендуется):
docker compose up -d
# Откройте http://localhost:8000

# Без Docker:
pip install -r requirements.txt
# Запустите PostgreSQL и обновите DATABASE_URL в .env
python backend/main.py
```

---

## ☁️ Деплой на Render.com

1. Загрузите проект на GitHub
2. Создайте **PostgreSQL** базу данных на Render → скопируйте Internal Database URL
3. Создайте **Web Service** → подключите репозиторий
4. **Runtime**: Docker
5. Добавьте переменные окружения:

| Переменная | Значение |
|---|---|
| `DATABASE_URL` | Internal URL из вашей Render PostgreSQL |
| `SECRET_KEY` | Случайная строка 32+ символа |
| `ADMIN_PASSWORD` | Пароль для /admin |
| `PAYMENT_CALLBACK_URL` | `https://your-app.onrender.com/api/payment/callback` |

---

## 📂 Структура проекта

```
import/
├── backend/
│   └── main.py              # FastAPI приложение
├── templates/
│   ├── index.html           # Главная страница
│   ├── products.html        # Каталог товаров
│   ├── admin.html           # Панель администратора
│   ├── add_product.html     # Добавление товара
│   ├── legal.html           # Юридические документы (4 в 1)
│   └── payment_stub.html    # Заглушка оплаты
├── static/
│   ├── images/              # Статичные изображения
│   ├── uploads/             # Загруженные фото товаров
│   └── favicon/             # Иконки сайта
├── Dockerfile               # Для деплоя на Render
├── docker-compose.yml       # Локальная разработка
├── requirements.txt
└── .env.example
```

---

## ✏️ Юридические документы

Замените тексты-заглушки в `templates/legal.html`:
- Поиск секции `#doc-terms` → Пользовательское соглашение
- Поиск секции `#doc-privacy` → Политика конфиденциальности
- Поиск секции `#doc-offer` → Договор оферты
- Поиск секции `#doc-returns` → Политика возврата

Все секции помечены комментариями `⬇️ ЗАМЕНИТЕ` и `⬆️ КОНЕЦ ЗАГЛУШКИ`.

---

## 💳 Подключение эквайринга

Откройте `backend/main.py` → функция `create_payment()` (~строка 470).

Там находится блок с комментарием «ЗДЕСЬ ИНТЕГРИРУЙТЕ РЕАЛЬНЫЙ ЭКВАЙРИНГ».

**ЮKassa:**
```python
pip install yookassa
# Раскомментируйте блок в create_payment()
# Установите PAYMENT_SHOP_ID и PAYMENT_SECRET_KEY в .env
```

**Stripe:**
```python
pip install stripe
# Используйте stripe.PaymentIntent.create()
# Установите PAYMENT_API_KEY в .env
```

---

## 🔒 Безопасность

- Пароли хранятся как PBKDF2-SHA256 хэш (100 000 итераций)
- JWT токены для авторизации
- Смените `SECRET_KEY` и `ADMIN_PASSWORD` перед деплоем!

---

## 👤 Аккаунты по умолчанию

| Роль | Логин | Пароль |
|---|---|---|
| Демо-пользователь | `demo` | `demo123` |
| Администратор | `admin` | из `ADMIN_PASSWORD` |

**Измените пароли после первого входа!**
